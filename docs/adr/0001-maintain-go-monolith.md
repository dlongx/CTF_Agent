# ADR-0001:继续维护Go单体

- 状态:Accepted
- 日期:2026-07-13

## 背景

现有Go服务、原生前端、Docker执行链和文件存储职责可辨，既有测试与构建基线可用。项目定位为个人本机长期维护，而非公网多租户平台。

## 决策

继续维护并系统加固现有Go单体，不重写，不恢复旧Python后端。首期保留原生前端和单机Docker。

## 后果

优先投入生命周期、密钥、超时、恢复、测试和文档。维持较低运维复杂度，但不支持水平扩展、多进程写入和复杂权限模型。达到明确阈值后通过新ADR重新评估。
