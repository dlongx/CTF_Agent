# ADR-0002:首期保留版本化文件存储

- 状态:Accepted
- 日期:2026-07-13

## 背景

当前任务数据天然包含附件、日志和WP，本机部署没有复杂查询或多进程写入要求。立即引入数据库会扩大迁移和运维范围。

## 决策

保留文件存储，为`meta.json`增加schema版本、原子替换、`.bak`恢复、幂等迁移和日志轮转，并提供带校验清单的备份恢复脚本。

## 后果

实现简单且任务目录可直接检查。跨任务查询仍需扫描，单服务进程是唯一写入者。超过10,000任务、需要复杂查询或多进程写入时，以新ADR评估SQLite。
