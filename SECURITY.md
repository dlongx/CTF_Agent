# 安全策略

## 支持范围

安全修复仅应用于`main`最新版本。项目当前定位是受信任用户的个人本机部署，不支持直接暴露到公网或不受信任的多租户场景。

## 报告方式

请通过仓库维护者提供的私有安全报告渠道提交问题，不要在公开Issue中粘贴API Key、Provider URL、真实题目附件、日志或可直接利用的细节。报告应包括影响、复现步骤、受影响提交和建议缓解措施。

## 当前边界

- 非回环监听必须配置`CTF_AGENT_ACCESS_TOKEN`。
- 任务附件不可信，只能在受限Docker容器中处理。
- Provider密钥不得进入工作区、任务日志、元数据、容器创建配置或命令行参数。
- `/health`和`/ready`不需要认证，但只返回脱敏状态；其他页面和API受访问令牌保护。
- Provider连通性测试只请求`/models`，不会发送任务内容或主动消耗模型推理额度。

部署检查和事故处理见[运维手册](docs/OPERATIONS.md)。
